// Public-facing URLs and app metadata. Update REPO if you rename the GitHub repo.
export const APP_VERSION = '1.0.0';
export const CONTACT_EMAIL = 'wonmor@gmail.com';

const GH_USER = 'wonmor';
const REPO = 'pstar-prep';

export const SITE_URL = `https://${GH_USER}.github.io/${REPO}/`;
export const PRIVACY_URL = `${SITE_URL}privacy.html`;
export const SOURCE_URL =
  'https://tc.canada.ca/en/aviation/publications/student-pilot-permit-private-pilot-licence-foreign-military-applicants-aviation-regulations-tp-11919';
